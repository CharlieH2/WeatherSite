{% extends "base.html.jinja" %}
{% block title %}Weather{% endblock %}
{% block head %}
    {{ super() }}
    <style type="text/css">
        .important { color: #336699; }
    </style>
{% endblock %}

{% block navbar %}
    <h1>Weathersite</h1>
{% endblock %}
{% block content %}
    <div class="weather-dashboard">
        
        <section class="current-weather">
            <h2 id="location-name">{{ location_name | default('London') }}</h2>
            <div class="current-temp">
                <span class="temp">{{ current.temperature | default('--') }}&deg;C</span>
                <span class="condition">{{ current.condition | default('Clear') }}</span>
            </div>
            <div class="current-details">
                <p>Wind: {{ current.wind_speed | default('--') }} mph</p>
                <p>Humidity: {{ current.humidity | default('--') }}%</p>
            </div>
        </section>

        <section class="hourly-forecast">
            <h3>Hourly Forecast</h3>
            <div class="hourly-scroll">
                {% for hour in hourly_data %}
                <div class="hour-card">
                    <p class="time">{{ hour.time }}</p>
                    <p class="icon">🌤️</p> <p class="temp">{{ hour.temperature }}&deg;C</p>
                </div>
                {% else %}
                <p>Hourly data unavailable.</p>
                {% endfor %}
            </div>
        </section>

        <section class="daily-forecast">
            <h3>7-Day Forecast</h3>
            <div class="daily-list">
                {% for day in daily_data %}
                <div class="day-row">
                    <span class="day-name">{{ day.date }}</span>
                    <span class="day-icon">🌧️</span>
                    <span class="day-temps">H: {{ day.max_temp }}&deg; L: {{ day.min_temp }}&deg;</span>
                </div>
                {% else %}
                <p>Daily data unavailable.</p>
                {% endfor %}
            </div>
        </section>

    </div>
{% endblock %}